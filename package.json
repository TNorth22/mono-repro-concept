{
  "name": "mono-repo-concept",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf packages/*/node_modules",
    "link-mobile": "/packages/core/build"
  }
}
